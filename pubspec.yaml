name: eetu_portfolio
description: Eetu Kallioniemi Portfolio built with Jaspr
version: 1.0.0

environment:
  sdk: ^3.10.0

jaspr:
  mode: server

dependencies:
  jaspr: ^0.22.0
  jaspr_localizations: ^1.0.0+beta.4
  

dev_dependencies:
  build_runner: ^2.6.0
  jaspr_builder: ^0.22.2
  jaspr_cli: ^0.22.2
  lints: ^3.0.0
  build_web_compilers: ^4.4.6

flutter: 
  generate: true
